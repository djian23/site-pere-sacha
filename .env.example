# Port du serveur (défaut : 3000)
PORT=3000

# Secret JWT — généré automatiquement si absent, mais définissez-le en production
# JWT_SECRET=remplacez-par-une-chaine-aleatoire-longue

# NODE_ENV=production  (active les cookies sécurisés HTTPS)
